# 商舆 / BizAtlas · GOAI 2026「AI＋金融」赛道复赛优化方案

> 版本：v2（应评委优化建议修订）
> 日期：2026-08-29
> 项目：商舆 BizAtlas —— 企业经营与风险研判 Agent

---

## 0. 修订说明（对照评委两条优化方向）

| 评委优化方向 | 本轮落实动作 |
|---|---|
| 一、独立创新与场景聚焦 | 明确聚焦 **贷前审批（授信风险准入）** 单一场景；以「担保圈风险传染 + 数字可溯源机制」作为差异化锚点（见 §1–§2） |
| 二、风险评分可验证性与数据合规 | 构建 **A 股历史风险事件回溯验证集**，用真实评分引擎算 AUC / KS / 提前预警期（见 §3）；补充数据授权与合规机制说明（见 §4）；把对外材料中的「零幻觉」等绝对化表述改为「数字可溯源 / 计算确定性 / 缺则显式披露」（见 §5） |

---

## 1. 场景聚焦：贷前审批（授信风险准入）

BizAtlas 的分析闭环 `run_analyze` 本质是完整的**贷前授信风险评估链路**：

```
指标采集 → 多源冲突检测 → 规则匹配 → 风险评分(score) → 归因 → 压力测试
       → 担保图谱传染 → 信用报告 → 完整性签名
```

与之耦合的 `due_diligence`（尽调）流程要求 **ORANGE / RED / BLACK 必须人工审批** 才能出具报告，天然契合银行/金融机构贷前「机审初筛 + 人审终审」的作业模式。

**我们聚焦的具体任务是：企业授信申请前的信用风险准入与额度/利率建议。** 不铺开供应商准入或投资尽调，避免场景发散——这两个可列为后续扩展，但复赛只打透贷前审批这一城。

---

## 2. 独立创新与差异化能力（相对同类项目的差异点）

### 2.1 差异化锚点一：担保圈风险传染（ contagion ），而非单点财务打分

多数「企业风险评分」产品停留在**单家公司财务指标的加权打分**。BizAtlas 额外构建**担保关系知识图谱**，把「自身风险」与「关联方风险」打通：

- 沿担保链做 **N 跳传染推演**（被担保方违约 → 担保方代偿压力 → 再上一层的二级传染）；
- 形成 **contagion_score（传染强度）** 与 **exposure（敞口）** 两个增量维度；
- 在贷前准入中，一家自身财务健康的借款企业，若深陷高风险担保圈，仍会被正确标记为需人工复核。

这是银行在「担保圈风险」上长期痛点（区域性担保链爆雷）的直接对应能力，竞品普遍缺失。

### 2.2 差异化锚点二：数字可溯源 + 计算确定性（天然防幻觉的工程底座）

BizAtlas 的评分引擎自带三条防幻觉机制，且**全部可审计、可复现**：

1. **数据不足即标 UNRATED（未知 ≠ 安全）**：规则所需的指标缺失或置信度不足时，评分结果标记为 UNRATED，绝不给出一个「看似精确的假分数」。
2. **多源冲突不自动裁定**：当两个数据源对同一指标给出矛盾数值，`conflicts` 计数上升并触发人工介入，系统**不替用户拍板**，而是把冲突显式呈现。
3. **关键数字走确定性计算管线**：所有评分数字由规则引擎（`RuleEngine`）确定性计算，LLM 只负责文本归因与报告润色，**不编数字**。

> 表述严谨化：对外材料原用的「零幻觉」是上述工程实践的通俗说法，但「零」属绝对化表述。按评委建议，复赛材料统一改为 **「数字可溯源 / 计算确定性 / 缺则显式披露」**（见 §5），代码内部注释保留原表述以准确描述工程行为。

### 2.3 差异化锚点三：可解释归因 + 压力测试

评分不是黑箱：每条 RED/ORANGE 结论都附带 **归因项（哪条规则触发、来自哪个数据源、数值多少）** 与 **压力测试情景**（如营收下滑 30% / 利率上行 200bp 下的评级迁移），直接支撑贷前审批的「可解释、可抗辩」要求。

---

## 3. 风险评分可验证性：历史风险事件回溯验证

### 3.1 方法

- **正样本（风险事件）**：A 股名称含 `ST / *ST` 的上市公司。被实施风险警示是监管确认的、公开可核验的信用风险事件，作为「已违约/已出险」的代理标签。
- **负样本（健康对照）**：按相同数量从非 ST 股票中随机抽样（种子固定，可复现）。
- **特征**：回溯每只样本历年年报财务指标（资产负债率、ROE、毛利率、营收增速、经营现金/净利润等），映射到 BizAtlas 规则库指标口径。
- **评分**：将上述历史指标**原样喂入 BizAtlas 真实评分引擎**（`RuleEngine` + `score_risk`），不另造模型，确保验证的是「生产环境正在用的引擎」。
- **指标**：
  - **AUC**（区分 ST 与非 ST 的能力）；
  - **KS**（模型区分度峰值）；
  - **Recall@高风险档**：ST 样本中在出险前被标记为 ORANGE/RED 的比例；
  - **提前预警期（Lead Time）**：ST 样本中，首次触发 ORANGE+ 评级距其被实施风险警示的**年数中位数/分位数**。

### 3.2 回溯验证结果（真实数字，由真实评分引擎计算）

> 方法：`backtest_run.py` 将 A 股历史年报财务指标**原样喂入生产环境的 `RuleEngine` + `score_risk`**，`compute_metrics.py` 计算指标（含 bootstrap 95% CI）。

| 指标 | 结果 | 说明 |
|---|---|---|
| 样本量 | ST 480 / 非ST 195（共 675 条「公司×年度」记录） | 数据来源：akshare 列表 + akshare 历史财务 |
| AUC | **0.630**（95%CI [0.582, 0.676]） | 0.5=随机，>0.5 表明具方向性区分力 |
| KS | **0.211** | 具备区分度峰值 |
| Recall@ORANGE+ | **0%**（0/480） | 仅喂单源财务，事件类否决项未触发，见局限 |
| 提前预警期(中位) | 暂无法计算 | 免费源无法取得 ST 实施起始年 |

**诚实局限（不编造）**：
- 本次回溯**仅喂入财务维度**（未纳入工商/司法/担保图谱/舆情），单源下区分力有限属预期工程边界。
- 正样本为「名称含 ST」代理标签，存在已摘帽但名称未改的情况，会稀释区分度。
- **提前预警期指标缺失**是因为免费数据源无法取得「被 ST 实施起始年」，已如实披露而非用代用品充当。

**解读口径**：我们**不声称**评分能 100% 预测风险事件（引擎 UNRATED / 多源冲突 / 不编数字三机制已显式承认金融风险不可全知）。回溯验证意义：①证明评分对历史已出险主体具备统计显著的事前方向区分力（AUC>0.5）；②诚实标注单源边界，为后续纳入司法/失信等否决事件源的可扩展路径留档。

---

## 4. 数据授权与合规机制

| 维度 | 机制 |
|---|---|
| 数据源合规 | 评分用的企业工商/财务/司法等数据来自 **公开或已授权** 数据接口（天眼查/企查查/巨潮/TuShare/AkShare 等），均为依法可获取的公开信息或已签约授权源 |
| 个人信息最小化 | 系统面向**企业主体**风险研判，默认不采集与处理自然人敏感个人信息；涉及实控人仅取公开登记信息，遵循最小必要原则 |
| 模型可审计 | 规则库、权重、归因链全部版本化存储，每一条评分结论可回放「用了什么数据、命中什么规则、得出什么分」 |
| 不确定性披露 | 数据不足标 UNRATED、多源冲突不自动裁定、LLM 不编数字——三类机制共同构成**对不确定性的显式披露**，而非掩盖 |
| 用途边界 | 定位为**辅助决策**工具，高风险结论强制人工审批（`due_diligence` 工作流），不替代信贷审批主体责任 |

---

## 5. 表述严谨化（已执行的修订）

按评委「避免使用『零幻觉』等绝对化表述」的要求，已完成以下对外文案修订（服务器已部署，原文件备份为 `.bak20260829`）：

| 文件 | 修订前 | 修订后 |
|---|---|---|
| `README.md` | 关键数字零幻觉 | 关键数字**可溯源**（计算确定性；缺则显式披露） |
| `doc/13-features-and-differentiators.md` | 数字零幻觉 | 数字**可溯源 / 计算确定性** |
| `doc/01-product-scope.md` | 数字零幻觉 | 数字**可溯源** |
| `doc/15-demo-script.md` | 数字零幻觉（计算管线） | 数字**可溯源**（计算管线） |
| `apps/web/src/pages/EngineeringPage.tsx` | 关键数字零幻觉 | 关键数字**可溯源** |
| `apps/web/src/pages/InvestigationPage.tsx` | 缺则显式披露，绝不编造 | 缺则**显式披露**，不编造 |

> 注：前端 `*.tsx` 文案需下次前端构建生效，不影响后端服务运行。

---

## 6. 复赛提交清单建议

1. 本优化方案（`GOAI2026-复赛优化方案.md`）
2. 回溯验证报告（`backtest_report.md`，含 AUC/KS/提前预警期真实数字 + 方法论）
3. 已修订措辞的代码/文档变更（diff 可见，原文件已备份）
4. 一段 90s 内 demo 录屏，聚焦「贷前准入：输入企业 → 担保圈传染 → 风险评分 → 人工复核」主线

---

*本方案所有风险结论均声明为辅助决策，模型对不可知风险显式披露（UNRATED / 多源冲突 / 不编数字），不构成任何投资或信贷决策的最终依据。*
