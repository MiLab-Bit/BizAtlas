# Python
__pycache__/
*.pyc
.venv/
bizvenv/
venv/

# Local data & DB (runtime artifacts at repo root, NOT packages/bizatlas/data source dir)
/data/
*.sqlite
*.db

# Exports & integrity receipts (generated at runtime)
/exports/
*.integrity.json

# Uploaded documents (runtime artifacts)
/uploads/

# Test / coverage
.coverage
.coverage*
.cov*
htmlcov/
.pytest_cache/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Secrets (keep .env.example tracked, ignore real .env)
.env
.env.*

# Frontend (Vite/React build artifacts & deps)
node_modules/
dist/
apps/web/dist/
*.tsbuildinfo
.vite/

# Local secrets / credentials — added 2026-08-27 git bootstrap (do NOT commit)
cfd_creds.json
.cf_token
config.yml
backups/

# ---------------------------------------------------------------------------
# 手工备份文件（*.bakYYYYMMDD* 等）：内容已由 git 历史覆盖，不入库
# ---------------------------------------------------------------------------
*.bak
*.bak[0-9]*
*.bak[0-9]*[a-z]
*.orig
*.rej

# ---------------------------------------------------------------------------
# 运行/调试日志与一次性脚本产物（backtest、前端构建日志等）
# ---------------------------------------------------------------------------
*.log
*.jsonl
fe_*.log
vite_*.log
/backtest_run.py
/scripts/backtest_run.py.local
exports/*.jsonl

# ---------------------------------------------------------------------------
# pnpm 本地配置：.npmrc 含 dangerously-allow-all-builds=true，
# 属本机放行开关，不入仓库；正式放行走 package.json 的 pnpm.allowBuilds
# ---------------------------------------------------------------------------
apps/web/.npmrc
apps/web/pnpm-workspace.yaml
