# 报告模板目录

模板 = 章节结构 + 插槽绑定（见 PRD 08 · ADR-001）。

MVP 计划文件：

| 文件 | 用途 |
|---|---|
| `risk_onepager.yaml` | 一页风险摘要 |
| `credit_assessment.yaml` | 企业信用评估报告 |

插槽只绑定 `MetricPack` / `RiskResult` 已有字段，禁止模板侧编造数字。
